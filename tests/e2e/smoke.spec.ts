import { test, expect } from '@playwright/test'

/**
 * Smoke test placeholder — full E2E tests require the Electron app to be
 * running. These will be expanded in Phase 7 using playwright-electron or
 * a local dev server for renderer-only testing.
 */
test('smoke: Playwright is configured correctly', async () => {
  expect(true).toBe(true)
})
